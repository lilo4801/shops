<h1><%= line_item.quantity %></h1>
<%= image_tag(line_item.book.image_url(:thumb)) %>
<h1><%= line_item.book.namebook %></h1><br>
Description:<%= truncate(line_item.book.description, length: 140) %><br>
<strong><%= number_to_currency(line_item.total_price) %></strong><br>
<%= link_to 'Remove Item', line_item, method: :delete, data: { confirm: "Are you sure? " } %>
